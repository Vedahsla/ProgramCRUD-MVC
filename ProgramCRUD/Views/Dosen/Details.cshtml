@model ProgramCRUD.Models.Dosen

@{
    ViewData["Title"] = "Dosen Details";
}

<h1>Dosen Details</h1>

<hr />

<div>
    <dl class="row">
        <dt class="col-sm-2">DosenID</dt>
        <dd class="col-sm-10">@Model.DosenID</dd>

        <dt class="col-sm-2">Name</dt>
        <dd class="col-sm-10">@Model.Name</dd>

        <dt class="col-sm-2">NIP</dt>
        <dd class="col-sm-10">@Model.NIP</dd>

        <dt class="col-sm-2">Gender</dt>
        <dd class="col-sm-10">@Model.Gender</dd>

        <dt class="col-sm-2">Telephone</dt>
        <dd class="col-sm-10">@Model.Telephone</dd>

        <dt class="col-sm-2">Address</dt>
        <dd class="col-sm-10">@Model.Address</dd>

        <dt class="col-sm-2">Active</dt>
        <dd class="col-sm-10">@(Model.Active ? "Yes" : "No")</p>
    </dl>
</div>

<a class="btn btn-secondary" asp-action="Index">Back to List</a>
<a class="btn btn-primary" asp-action="Edit" asp-route-id="@Model.DosenID">Edit</a>
<a class="btn btn-danger" asp-action="Delete" asp-route-id="@Model.DosenID">Delete</a>
